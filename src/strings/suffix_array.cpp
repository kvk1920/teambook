#include <ngu_teambook/strings/suffix_array.hpp>

#include <algorithm>

namespace ngu_teambook::strings
{
    std::vector<int> BuildSuffixArray(std::string_view s)
    {
        std::vector<int> sa;
        return sa;
    }
}